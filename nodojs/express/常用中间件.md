

## express.static

Express 内置的中间件函数，用于`向客户端提供静态文件`，如 HTML、CSS、图片和客户端 JavaScript


### 基本使用
```js
const express = require('express')
const app = express()

// 将 'public' 目录下的文件作为静态资源提供服务
app.use(express.static('public'))

app.listen(3000)
```

客户端访问

- `public/images/kitten.jpg` → `http://localhost:3000/images/kitten.jpg`
- `public/css/style.css` → `http://localhost:3000/css/style.css`

注意：public不会出现url中



### 增加虚拟前缀
```js
// 通过 '/static' 前缀访问 public 目录下的文件
app.use('/static', express.static('public'))
```
- `public/images/kitten.jpg` → `http://localhost:3000/static/images/kitten.jpg`



### 使用绝对路径更好
`express.static()` 接收的路径是相对于启动 Node 进程的目录。为了在不同环境下稳定运行，建议使用绝对路径
```js
const path = require('path')
app.use('/static', express.static(path.join(__dirname, 'public')))
```


### 配置多个静态目录





### 生产建议通过方向代理/cdn的形式暴露静态资源，以便于提高访问性能



Nginx
为什么使用nginx会比直接访问服务器性能更好?




cdn：
 * 非业务的访问服务器会增加不必要的性能开销
 * 托管到云服务厂商与业务解耦，访问效率也高，cdn是分布式，有一定的访问性能支撑


