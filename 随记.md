## 看错年份了，草

```
               console.log(moment(current).format('YYYY-MM-DD'))

                      // 获取今天的日期（0点）

                      const today = moment().startOf('day')

                      // 否则，只能选择大于等于今天的日期

                      console.log(current && current < today)

                      return current && current < today
```

## 函数中的依赖的状态是过期的
因为使用useMome函数做了缓存，所以依赖是旧的，所以函数中应用到依赖正常情况是要写到依赖收集的数组中

## 主动受控组件？



## react

初始化时
避免setData与函数同时调用，函数内无法获取到setData之后的结果，导致状态滞后

## 切换网络节点后git无法提交？


