

1. 保留公共部分，暴露自由部分



```tsx
     <AdminTable
        containerId={props.outerWrapId}
        request={tableSource}
        columns={columnsData}
        actionRef={proTableRef}
        formRef={formRef}
        headerTitle="医保取数逻辑表"
        toolBarRender={() => [
          (rbac.includes('financeBasicInformationInsuranceAdd')&&
            <Button className={`button-color-green ${styles.btn}`} key="add" onClick={() => setShowAdd(true)}>
            新增
          </Button>),
          (rbac.includes('financeBasicInformationInsuranceImport')&&
            <Dropdown menu={{ items }} key="upLoad" placement="bottom">
            <Button  className={`button-color-sunset ${styles.btn}`} >
              批量上传
              <DownOutlined />
            </Button>
          </Dropdown>
          ),
          ( rbac.includes('financeBasicInformationInsuranceExport')&&
            <Dropdown key={1}  menu={{items:exportExcel }} placement="bottom" >
            <Button  type="primary" loading={loadingStatus.export} >
              导出数据
              <DownOutlined />
            </Button>
          </Dropdown>
          )
        ]}
      />
```

传入的属性都是自由的，非自由的已经固定到AdminTable组件里